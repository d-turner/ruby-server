#!/bin/bash
# run the server with the first given argument
# if none given then run the server with the default port 3001
# no compilation necessary
# System dependencies: ruby 2.0.0 or later
ruby server.rb $1
